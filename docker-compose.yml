services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: spaced
      POSTGRES_PASSWORD: spaced_dev
      POSTGRES_DB: spaced_english
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
